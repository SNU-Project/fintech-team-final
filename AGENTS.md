# 🚨 잠깐! 코드를 고치기 전에 `CLAUDE.md`를 먼저 읽으세요

이 파일은 요약본입니다. **전체 규칙과 배경은 [`CLAUDE.md`](CLAUDE.md)에 있습니다.**
(같은 내용을 두 곳에 적으면 반드시 어긋나므로, 여기에는 절대 규칙만 둡니다.)

---

## 절대 규칙 3가지

### 1. 데이터를 지어내지 마세요

API가 실패해도 난수·사인파·보간으로 값을 만들지 마세요. 예외를 던지고 멈추거나 "데이터 없음"을 표시하세요.

```python
# 🚫 실제로 이 코드 때문에 백테스트가 전부 난수였습니다
prices = 10000 * np.cumprod(1 + np.random.normal(rate, 0.005, len(dates)))
```

```js
// 🚫 "과거 주가"라면서 사인파를 그리고 있었습니다
const wave = 1 + Math.sin(step * 0.82) * asset.risk * 0.08;
```

이 프로젝트는 **데이터가 진짜인 것**이 유일한 차별점입니다. 그럴듯한 곡선을 만들어 넣는 순간 존재 이유가 사라집니다.

### 2. 빌드 도구를 추가하지 마세요

npm, webpack, vite, TypeScript, React, 차트 라이브러리 전부 금지. 순수 HTML/CSS/JS + 인라인 SVG만 씁니다. 발표 당일 CI가 깨지면 복구할 시간이 없습니다.

### 3. 수익률·물가를 하드코딩하지 마세요

모든 수치는 `data/*.json`에서 읽습니다. 유일한 예외는 예금 수익률이며, 화면에 `가정값` 배지로 명시되어 있습니다.

---

## 빠른 참고

```bash
python3 -m http.server 5173                                    # 실행
python3 pipeline/fetch_data.py && python3 pipeline/transform.py  # 데이터 갱신
```

고친 뒤 반드시 확인:
- 콘솔 에러 0건
- **모바일 375px 가로 스크롤 없음** (CSS 그리드는 `1fr` 말고 `minmax(0, 1fr)`)
- 라이트/다크 양쪽
- 새 수치는 외부 사실과 교차 검증

**발표 2026-08-21. 8/18부터 기능 추가 동결, 버그 수정만.**

자세한 내용 — 이미 밟은 지뢰 5가지, CORS 실측 결과, 데이터 파이프라인 설명 — 은 [`CLAUDE.md`](CLAUDE.md)를 보세요.
