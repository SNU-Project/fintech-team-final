# 🚨 잠깐! 코드를 고치기 전에 이것부터 읽으세요

**이 프로젝트는 "데이터가 진짜인 것"이 유일한 차별점입니다.**
발표(2026-08-21) 동료평가 기준에 "데이터 수집·정제의 충실도" 항목이 있고, 우리 팀은 **AI가 만든 가짜 데이터 3건을 잡아내고 실데이터로 교체한 과정**을 발표의 핵심으로 삼습니다.

그래서 아래 3가지는 **절대 규칙**입니다. 어기면 프로젝트의 존재 이유가 사라집니다.

---

## ❌ 절대 하지 말 것 (3가지)

### 1. 데이터를 지어내지 마세요 — 가장 중요

API가 실패하거나 값이 비어 있어도 **절대로** 다음을 하지 마세요:

```python
# 🚫 금지 — 실제로 이 코드 때문에 백테스트가 전부 난수였습니다
prices = 10000 * np.cumprod(1 + np.random.normal(rate, 0.005, len(dates)))
```

```js
// 🚫 금지 — "과거 주가"라면서 사인파를 그리고 있었습니다
const wave = 1 + Math.sin(step * 0.82) * asset.risk * 0.08;
```

**올바른 처리**: 예외를 던지고 멈추거나(`pipeline/`), 화면에 "데이터 없음"을 표시하세요(`assets/js/`). 값이 없으면 없다고 하는 게 맞습니다. 그럴듯한 곡선을 만들어 넣는 순간 이 프로젝트는 실격입니다.

보간(interpolation)도 하지 마세요. 결측 월은 그냥 빼고, 뺐다는 사실을 `notes`에 남깁니다.

### 2. 빌드 도구를 추가하지 마세요

npm, webpack, vite, TypeScript, React — **전부 금지**입니다. 화면은 순수 HTML/CSS/JS + 인라인 SVG만 씁니다.

유일한 예외는 `api/insight.mjs`(Vercel 서버리스 함수)이며, 이것도 빌드 없이 그대로 배포됩니다. **여기에도 npm 의존성을 추가하지 마세요.**

이유: 발표 당일 평가자가 배포 URL을 직접 엽니다. 빌드 단계가 없으면 CI가 깨질 일이 없습니다. 실제로 팀원 중 한 명의 Next.js 프로토타입은 `basePath` 불일치로 Pages에서 404가 났습니다.

차트 라이브러리도 추가하지 마세요. `assets/js/charts.js`에 필요한 건 이미 다 있습니다.

### 3. 수익률·물가를 하드코딩하지 마세요

```js
// 🚫 금지
const RISK_RETURNS = { stable: 0.03, balanced: 0.06, aggressive: 0.09 };
```

모든 수치는 `data/market.json`, `data/cpi.json`에서 읽습니다. 이 파일들은 GitHub Actions가 주 1회 실제 API에서 받아 PR로 갱신합니다.

**유일한 예외**는 예금 수익률(`pipeline/transform.py`의 `CASH_ANNUAL_RETURN = 0.030`)입니다. 시장 시세가 없어서 가정값이고, 화면에 `가정값` 배지로 명시하고 있습니다. 새 가정값을 추가하려면 반드시 화면과 `meta.json`의 `assumptions`에 함께 노출하세요.

---

## 이 프로젝트가 뭔가

**샐러리갭** — "연봉은 올랐는데 왜 더 빠듯할까?"

물가를 반영한 내 연봉의 실질 가치를 진단하고, 실제 10년 시장 데이터로 부족분을 메울 자산배분을 설계하는 정적 웹앱입니다.

- 라이브: https://fintech-team-final.vercel.app/
- 서울대 빅데이터 핀테크 13기 · 웹 개발 및 시각화 팀 프로젝트
- 팀원 5명의 개별 프로토타입을 하나로 통합한 결과물 (통합 내역은 `README.md` 6절)

**흐름**: 홈에서 5단계 설문 → 연봉 성적표(100점) → 아래 4개 탭의 상세 리포트

1. **내 물가** — 소비 습관 기반 개인 물가지수 (우리 팀의 차별화 기능)
2. **실질임금 진단** — 연봉 vs 물가, 협상 가이드
3. **목표 자산 모으기** — 목표 역산, 성장 곡선
4. **자산 타임머신** — 실제 종가 기반 백테스트

---

## 구조

```
index.html                  마크업 (패널 6개: home, mine, gap, goal, time, final)
assets/css/styles.css       디자인 시스템 (CSS 변수, 라이트/다크)
assets/js/
  charts.js                 SVG 차트 — barChart, hBarChart, lineChart, contributionChart
  engine.js                 계산 로직 — 순수 함수, DOM 안 건드림
  live.js                   실시간 API 레이어
  personas.js               가구 유형별 지출 시작값 (국가데이터처 가계동향조사)
  app.js                    화면 조립, 이벤트 바인딩
api/
  insight.mjs               AI 해설 서버리스 함수 (Vercel) — 아래 설명 참고
data/                       ← 손으로 고치지 마세요. Actions가 덮어씁니다
  market.json               자산 시계열·지표·포트폴리오
  cpi.json                  물가 (전체 + COICOP 12품목)
  meta.json                 출처·가정·정제 내역
pipeline/
  fetch_data.py             수집 (표준 라이브러리만 사용, 의존성 0)
  transform.py              정제·지표 산출
docs/
  pr-protocol.md            PR 올리기 전 자체 점검 (AI에게 읽히세요)
  ai-usage.md               AI 활용 및 검증 내역 (채점 7번 대응)
  presentation.md           발표 구성·데모 시나리오
```

### `api/insight.mjs` — 유일한 서버 코드

화면은 정적이지만 **AI 해설 하나만 서버리스 함수**입니다. Vercel에서만 동작하고, GitHub Pages 배포본에는 이 기능이 없습니다(Pages는 함수를 못 돌립니다).

여기 있어야 하는 이유는 하나입니다 — **AI Gateway 토큰을 브라우저에 노출할 수 없어서**입니다. 앞으로도 키가 필요한 기능은 전부 여기로 보내세요. `assets/` 안에 키를 넣으면 그대로 공개됩니다.

**환경변수**: Vercel 프로젝트에 `GEMINI_API_KEY`가 필요합니다(Google AI Studio 무료 키).
Vercel AI Gateway를 쓰다가 옮겼는데, 무료 사용량이 있어도 **신용카드 등록을 요구**해서
403이 났기 때문입니다. AI Studio는 카드 없이 무료 할당량을 줍니다.
키가 없으면 화면은 검증된 기본 해설로 정상 동작합니다.

이 함수는 모델 출력도 검증합니다. **전달한 값에 없는 숫자가 응답에 나오면 통째로 버립니다**(`safeText`). "데이터를 지어내지 않는다"는 원칙을 AI 출력에까지 적용한 것이니, 이 검증을 약화시키지 마세요.

### 코드를 어디에 둘지

**계산은 `engine.js`에, 렌더링은 `app.js`에** 두세요. `engine.js`의 함수는 전부 순수 함수라 콘솔에서 바로 테스트할 수 있습니다.

---

## 데이터 규칙: 실시간 vs 스냅샷

브라우저에서 API를 직접 부르려면 그 API가 **CORS를 열어줘야** 합니다. 실측 결과:

| API | 용도 | CORS | 방식 |
|---|---|---|---|
| OECD SDMX | 물가 (전체 + 12품목) | ✅ | 브라우저 실시간 |
| Frankfurter (ECB) | 원/달러 | ✅ | 브라우저 실시간 |
| CoinGecko | 비트코인 | ✅ | 브라우저 실시간 |
| Yahoo Finance | 주식·금·채권 10년 시계열 | ❌ | Actions가 주 1회 수집 → PR |

**새 데이터 소스를 추가할 때는 반드시 CORS부터 실측하세요:**

```bash
curl -sI -H "Origin: https://fintech-team-final.vercel.app" "<API_URL>" | grep -i access-control-allow-origin
```

헤더가 없으면 브라우저에서 못 씁니다. `pipeline/`에 넣어 스냅샷으로 처리하세요. **프록시 서비스를 끼워 넣지 마세요** — 발표날 죽으면 대책이 없습니다.

API 키가 필요한 서비스는 되도록 피하세요. 화면이 쓰는 데이터는 전부 **키 없이** 돌아갑니다.

키가 꼭 필요하면 `api/` 안의 서버리스 함수에서만 쓰세요(현재 AI 해설이 유일). `assets/` 안에 넣으면 그대로 공개됩니다.

---

## ⚠️ 이미 밟은 지뢰 — 다시 밟지 마세요

같은 실수가 반복되고 있어서 적어둡니다. 자세한 내용은 `docs/ai-usage.md`.

1. **Yahoo 월봉 타임스탬프는 거래소 현지시간 기준입니다.** UTC로 그냥 변환하면 KST 10월 1일이 UTC 9월 30일로 밀려 **매년 10월이 9월을 덮어씁니다.** 응답의 `gmtoffset`을 반드시 적용하세요.

2. **이상치는 "크기"가 아니라 "모양"으로 거르세요.** 중앙값 대비 배율로 필터링했다가 비트코인의 **실제 관측치 50개월**을 삭제했습니다. 비트코인은 10년간 진짜로 100배 올랐습니다. 오류 틱은 "튀었다가 바로 되돌아오는" 모양이므로, 들어가는 변화율과 나오는 변화율이 반대 방향으로 극단적인 지점만 제거합니다 (`reject_outliers`).

3. **OECD를 연속 호출하면 429로 막힙니다.** 12품목을 하나씩 부르지 말고 `CP01+CP02+...` 다중 코드 문법으로 한 번에 받으세요 (`fetch_oecd_cpi_by_category`).

4. **CSS 그리드의 `1fr`은 `minmax(auto, 1fr)`입니다.** `auto` 최솟값이 자식의 min-content(숫자 input 기본 폭 ~400px)라 모바일에서 페이지 전체에 가로 스크롤이 생깁니다. 항상 **`minmax(0, 1fr)`** 을 쓰세요.

5. **필요 저축액은 올림하세요.** 내림하면 "100만원이면 된다"고 해놓고 정작 100만원으로는 목표에 못 닿는 모순이 생깁니다.

6. **로컬(`localhost`)에서는 OECD 실시간 조회가 실패합니다 — 버그가 아닙니다.** OECD는 허용된 출처에만 CORS 헤더를 돌려주는데, 배포 도메인(`https://fintech-team-final.vercel.app`)은 통과하고 `http://localhost:5173`은 막힙니다. 로컬에서는 콘솔에 CORS 에러가 뜨고 스냅샷 폴백으로 그려지는 게 정상 동작입니다. **실시간 동작을 확인하려면 배포본에서 보세요.** 이걸 "고치겠다고" 프록시를 끼워 넣지 마세요.

---

## 작업 방법

### 실행

```bash
python3 -m http.server 5173
```

### 데이터 갱신

```bash
python3 pipeline/fetch_data.py && python3 pipeline/transform.py
```

`data/_raw.json`은 중간 산출물이라 커밋하지 않습니다(`.gitignore`).

### 고치고 나서 반드시 확인할 것

- [ ] 브라우저 콘솔 에러 0건
- [ ] **모바일 375px에서 가로 스크롤 없음** — `document.body.scrollWidth === document.documentElement.clientWidth`
- [ ] 라이트/다크 양쪽 확인 (우상단 "테마" 버튼)
- [ ] 홈 설문 → 성적표 → 탭 4개 전부 렌더되는지
- [ ] 입력 엣지 케이스: 0, 음수, 극단값
- [ ] 새 수치를 넣었다면 **외부 사실과 교차 검증** (예: 금 2021.08→2026.08은 USD +140% × 환율 +22% ≈ +193%, 화면값 +194.4% ✅)

### 브랜치와 PR — `main`에 직접 push할 수 없습니다

`main`은 보호되어 있습니다. push하면 그 즉시 실서비스가 배포되기 때문에, 반드시 브랜치를 만들어 PR로 합칩니다.

```bash
git checkout main && git pull            # 최신 상태로 맞추기
git checkout -b feat/이름-작업내용         # 내 브랜치 만들기
#  ... 작업 ...
git add -A && git commit -m "무엇을 왜 고쳤는지"
git push -u origin feat/이름-작업내용
#  터미널에 뜨는 링크로 PR 생성 → 자동검사 통과하면 Merge
```

브랜치 이름은 `feat/`(기능), `fix/`(버그), `docs/`(문서) 뒤에 본인 이름과 작업을 붙입니다.
예: `feat/somin-차트툴팁`, `fix/yeju-모바일레이아웃`

**승인자는 0명**이라 검사만 통과하면 혼자 Merge할 수 있습니다.

올리기 전에 [`docs/pr-protocol.md`](docs/pr-protocol.md)를 읽고 자체 점검을 돌리세요. 회의 없이 굴리기 위한 최소 장치입니다.

### PR 자동검사 (`.github/workflows/pr-check.yml`)

PR을 올리면 아래를 자동으로 검사합니다. 하나라도 실패하면 합칠 수 없습니다.

| 검사 | 막는 것 |
|---|---|
| 가짜 데이터 | `np.random`, `Math.random`, `Math.sin`/`cos` |
| 빌드 도구 | `package.json`, vite, webpack, tsconfig 등 |
| 외부 CDN | `<script src="https://...">` |
| JS 문법 | `node --check` |
| 데이터 무결성 | 관측치·CAGR·변동성·MDD 범위, 품목 12개 |
| 하드코딩 수익률 | `RISK_RETURNS = {...}` 같은 패턴 |

검사에 걸렸다면 **우회하지 말고 원인을 고치세요.** 이 검사들은 실제로 이 프로젝트에서 터졌던 문제들입니다.

### 커밋

- 커밋 메시지에 **왜 그렇게 했는지**를 적으세요. 이 리포에는 대화 기록이 없어서 커밋 메시지가 유일한 맥락입니다.
- `data/` 갱신은 주 1회 Actions가 **PR을 열고 자동 머지**합니다. 사람이 할 일은 없습니다.
  (2026-08-10에 조직 소유자가 Actions의 PR 생성 권한을 켜 주면서 가능해졌습니다.
  그 전에는 브랜치 푸시까지만 하고 사람이 클릭해야 했습니다.)
- 워크플로 파일(`.github/workflows/`)을 수정하려면 토큰에 `workflow` 스코프가 필요합니다 (`gh auth refresh -s workflow`).

---

## 🔴 발표 3일 전부터 (2026-08-18 이후)

**기능 추가 동결.** 버그 수정만 하세요.

평가자가 발표 당일 배포 URL을 직접 열어 채점합니다. 이 시점에 배포가 깨지면 복구할 시간이 없습니다.

- `main`에 푸시하면 즉시 배포됩니다. 확인 없이 푸시하지 마세요.
- 데모 시나리오는 `docs/presentation.md`에 고정되어 있습니다. 입력 기본값(연봉 3600/3750, 타임머신 2021-08·1000만원, 생활 유형 "혼자 생활비를 관리해요")을 바꾸면 리허설이 어긋납니다.

---

## 톤

- 화면 문구는 **한국어**, 단정적이되 과장하지 않습니다.
- 숫자 옆에는 항상 **근거나 한계**를 같이 답니다. "수익률 14%"만 쓰지 말고 "최대 낙폭 −35.6%"를 나란히 보여주세요.
- 투자 권유로 읽힐 표현을 쓰지 마세요. 면책 문구는 `index.html`의 `#basis` 섹션에 있습니다.
- 코드 주석은 **"무엇"이 아니라 "왜"** 를 적습니다. 위 지뢰 5개가 전부 주석으로 남아 있는 이유입니다.
